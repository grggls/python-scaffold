Run the test suite with coverage analysis:

`uv run pytest --cov --cov-report=term-missing`

Report:
- Total number of tests and pass/fail count
- Overall coverage percentage
- Any lines missing coverage
- Whether the coverage threshold (80%) is met
